{
  "name": "slipperstrap",
  "version": "0.9.0",
  "description": "Bootstrap inspired scss framework to create classes more dynamically",
  "main": "dist/slipperstrap.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx sass --no-source-map scss/:dist/",
    "prepackageOnly": "rm -rf dist && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patrickguenther/slipperstrap.git"
  },
  "keywords": [
    "scss",
    "css"
  ],
  "author": "Patrick Günther",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/patrickguenther/slipperstrap/issues"
  },
  "homepage": "https://github.com/patrickguenther/slipperstrap#readme",
  "devDependencies": {
    "scss": "^0.2.4"
  },
  "dependencies": {}
}
